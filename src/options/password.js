export default ({
  name: 'registryPassword',
  type: 'string',
  prompt: {
    "type": 'password',
  },
  alias: 'u',
  message: 'Password',
  // validators: [{
  //   regex: "^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$"
  // }]
})
