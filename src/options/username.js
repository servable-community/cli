export default ({
  name: 'registryUsername',
  type: 'string',
  prompt: {
    "type": 'input',
  },
  alias: 'u',
  message: 'Username',
  // validators: [{}]
  // validators: [{
  //   regex: "^[a-zA-Z0-9](_(?!(\.|_))|\.(?!(_|\.))|[a-zA-Z0-9]){6,18}[a-zA-Z0-9]$"
  // }]
})
